{
  "name": "ncaa-draft",
  "version": "1.0.0",
  "description": "NCAA Tournament Fantasy Draft — KU Edition",
  "main": "server.js",
  "scripts": {
    "prestart": "node -e \"const fs=require('fs');let h=fs.readFileSync('public/index.html','utf8');h=h.replace(/BUILD_TIMESTAMP/g,new Date().toISOString());fs.writeFileSync('public/index.html',h);\"",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
